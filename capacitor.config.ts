import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Bateria Geropsicomotora',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
